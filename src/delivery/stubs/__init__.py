"""Service stubs the Activities call over HTTP.

Each stub stands in for an external dependency (payment, restaurant, driver).
They run as separate processes so the demo can take one offline and watch the
Workflow retry through the outage.
"""
